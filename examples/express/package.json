{
    "name": "no-backend-express",
    "version": "1.0.0",
    "description": "example of usage of no-backend middlewares for express",
    "main": "server.js",
    "scripts": {
        "test": "echo \"Error: no test specified\" && exit 1",
        "start": "node server.js",
        "clean-install": "rm -rf ./node_modules && rm -rf ./package-lock.json && rm -rf ./yarn.lock && npm install"
    },
    "keywords": [
        "express",
        "no-backend"
    ],
    "author": "Gherciu Gheorghe",
    "license": "ISC",
    "dependencies": {
        "express": "^4.16.4",
        "graphql": "^14.1.1",
        "no-backend": "^1.5.4"
    }
}
