const buildTablesSchema = (options) => {
    
    let tablesSchema;
    
    //connect to db 
    //get db tables and types
    //build tables schema 

    tablesSchema = {}

    return tablesSchema

}

export default buildTablesSchema